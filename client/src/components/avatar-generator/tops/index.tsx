/**
 * Index file for the top (hair, hats, etc.)
 */
// Imports of the top components

// Interface
interface TopProps {
  color: string;
  kind: string;
}

// Main eyebrows component
const Top = ({ color, kind }: TopProps) => {
  return;
};

// Export
export default Top;
