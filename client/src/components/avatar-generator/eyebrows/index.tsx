/**
 * Index file for the eyebrows
 */
// Imports of the eyebrows components

// Interface
interface EyebrowsProps {
  color: string;
  kind: string;
}

// Main eyebrows component
const Eyebrows = ({ color, kind }: EyebrowsProps) => {
  return;
};

// Export
export default Eyebrows;
